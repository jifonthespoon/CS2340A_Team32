<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/shoppingListItemName"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_marginStart="24dp"
        android:gravity="center"
        android:text="rferf"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@+id/decrButton"
        app:layout_constraintHorizontal_bias="0.005"
        app:layout_constraintStart_toStartOf="parent"
        tools:layout_editor_absoluteY="2dp" />

    <TextView
        android:id="@+id/quantity"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_marginEnd="12dp"
        android:layout_toStartOf="@+id/incrButton"
        android:gravity="center"
        android:text="10"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@+id/incrButton"
        app:layout_constraintStart_toEndOf="@+id/decrButton"
        tools:layout_editor_absoluteY="0dp" />

    <Button
        android:id="@+id/decrButton"
        android:layout_width="72dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="128dp"
        android:background="#00FFFFFF"
        android:text="-"
        android:textColor="#000000"
        android:textSize="24sp"
        app:layout_constraintEnd_toEndOf="@+id/incrButton"
        tools:layout_editor_absoluteY="0dp" />

    <Button
        android:id="@+id/incrButton"
        android:layout_width="72dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:background="#00FFFFFF"
        android:text="+"
        android:textColor="#000000"
        android:textSize="24sp"
        app:iconPadding="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        tools:layout_editor_absoluteY="0dp" />

</androidx.constraintlayout.widget.ConstraintLayout>