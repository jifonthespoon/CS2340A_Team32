package com.example.greenplate.viewmodels;

import static androidx.core.content.ContextCompat.startActivity;

import android.content.Intent;
import android.widget.Button;
import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

import com.example.greenplate.R;

public class recipeActivity {

    /**Button shoppingListNavButton = findViewById(R.id.shoppingListNavButton);
        shoppingListNavButton.setOnClickListener(v -> {
        Intent intent = new Intent(shoppingListNavButton.this, shopping_list.class);
        startActivity(intent);
    });
     **/
}
